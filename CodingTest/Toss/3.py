import sys
def solution(merchantNames):
    print(merchantNames)

    for i in range(1, len(merchantNames)):
        name = merchantNames[i]
        for j in range(len(name)):
            if name[j] != " ":
                continue


    answer = []
    return answer